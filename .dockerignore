# Keep host build artifacts out of the build context so the in-container
# cmake invocation in docker/orchestrator/Dockerfile is the sole source of
# truth for compile flags (e.g. ENABLE_AVX512=OFF).
build/
**/build/
.git/
.idea/
.vscode/
*.o
*.obj
